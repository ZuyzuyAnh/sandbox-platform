# Frontend production build variables — copy to frontend/.env.production
# Also set the same values in root .env.production for Docker build args.

# Leave empty when nginx serves frontend + /api on the same host (recommended).
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=
